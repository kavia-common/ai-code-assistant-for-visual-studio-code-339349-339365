# Required: absolute path to the sqlite_database canonical DB file (myapp.db)
# This env var is provided by the sqlite_database container outputs.
SQLITE_DB=/absolute/path/to/sqlite_database/myapp.db

# Server binding
HOST=0.0.0.0
PORT=3001

# Optional CORS controls (defaults allow all)
# ALLOWED_ORIGINS=*
